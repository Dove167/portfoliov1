---
interface Props {
  tag?: 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6' | 'p' | 'span';
  variant?: string;
  class?: string;
}

const { tag: Tag = 'p', variant, class: className } = Astro.props;
---

<Tag class={className}>
  <slot />
</Tag>
